# 实时视频生成时域一致性优化 —— Benchmark 选型与实验方案（细化版）

## 〇、总体目标与数据包定位

本实验体系的目标不是产出一张表，而是产出一个**完整可复现的实验数据包（Data Package）**，满足：

1. 支撑竞赛文档/论文的全部表格与图（16 图 9 表起）
2. 任何评委可凭数据包 100% 复现全部数字
3. 数据包自带完整使用说明，独立可用、可审计、可扩展

数据包命名：`RTVD-TC-DataPackage-v1.1/`（Real-Time Video Diffusion, Temporal Consistency）

---

## 一、Benchmark 选型（8 个对比方法 + 1 个 trivial baseline）

| #    | Benchmark                           | 流派              | 角色定位                                   |
| ---- | ----------------------------------- | ----------------- | ------------------------------------------ |
| 1    | StreamDiffusion (ECCV'24)           | 实时流式扩散      | 速度维度主敌（赛题指定仓库）               |
| 2    | Rerender-A-Video (SIGGRAPH Asia'23) | keyframe+传播编辑 | TC/质量维度主敌（赛题指定仓库）            |
| 3    | TokenFlow (ICLR'24)                 | 扩散特征一致性    | 特征一致性流派最强                         |
| 4    | vid2vid-zero                        | 零样本视频编辑    | cross-frame attention 代表（赛题指定仓库） |
| 5    | Text2Video-Zero (ICCV'23)           | 零样本文生视频    | 帧锚定注意力代表                           |
| 6    | ControlVideo (ICCV'23)              | 结构控制编辑      | ControlNet 流派代表                        |
| 7    | StableVideo (ICCV'23)               | 帧间传播编辑      | inter-frame propagation 代表               |
| 8    | AnimateDiff-LCM                     | 运动模块+少步数   | 运动先验路线代表                           |
| 9    | SD-Turbo 逐帧（trivial baseline）   | 无任何时域建模    | 下界参照，证明"逐帧必塌 TC"                |

> 硬性控制变量：统一 SD1.5 基座（差异处注明）、统一 RTX 4090、统一 prompt 集、统一 5 组种子、统一 512×512、统一 4 步采样（LCM 等价）。跑不动的方法用论文报告值并标注 †。

---

## 二、数据包总体结构规范（纯 Raw Data + XLSX 体系，不存任何视频/图像）

```
RTVD-TC-DataPackage-v1.1/
│
├── README.md                        # 总使用说明（入口文档）
├── MANIFEST.json                    # 全包文件清单 + SHA256 校验
├── VERSION                          # 数据包版本号
│
├── 00_docs/
│   ├── usage_guide_cn.md            # 中文完整使用说明（每个 xlsx 的字段字典）
│   ├── usage_guide_en.md            # 英文版
│   ├── method_descriptions.md       # 每个 benchmark 的方法简介+权重来源+许可
│   ├── hardware_environment.md      # 硬件/驱动/CUDA/依赖版本全记录
│   └── changelog.md                 # 实验记录变更日志
│
├── 01_configs/                      # 所有实验配置（复现的根）
│   ├── global_config.yaml           # 全局参数（分辨率/步数/种子/设备）
│   ├── methods_config.yaml          # 9 个方法的超参与权重路径
│   ├── prompts/
│   │   ├── prompt_set_a_simple.json # 简单词 prompt（每数据集 20 条）
│   │   ├── prompt_set_b_sentence.json
│   │   └── prompt_set_c_style.json
│   ├── seeds.json                   # 5 组全局种子
│   ├── video_splits/
│   │   ├── davis_test.json          # 测试视频 ID 清单
│   │   ├── deepfashion_test.json
│   │   └── webvid_test.json
│   └── environment_lock.yaml        # pip 冻结环境
│
├── 02_raw_data/                     # ★ 全量 Raw 数据，纯 XLSX，每个实验板块一个文件
│   ├── exp01_main_comparison.xlsx   # 主对比（约 4.4 万行 raw）
│   ├── exp02_latency.xlsx           # 实时性（约 4 万行 raw）
│   ├── exp03_tc_analysis.xlsx       # TC 深度分析（约 6 万行 raw）
│   ├── exp04_ablation.xlsx          # 消融 15 变体（约 1 万行 raw）
│   ├── exp05_robustness.xlsx        # 鲁棒性（约 1.5 万行 raw）
│   ├── exp06_efficiency.xlsx        # 效率/部署（约 2 千行 raw）
│   ├── exp07_generalization.xlsx    # 泛化（约 8 千行 raw）
│   └── exp08_user_study.xlsx        # 用户研究（约 4 千行 raw）
│
├── 03_figures/                      # 全部出版级图（PDF/PNG 双格式）
│   └── fig01_radar.pdf ... fig16_user_preference.pdf
│
├── 04_tables/                       # 全部表格（XLSX + LaTeX 双格式）
│   └── table01_main_comparison.xlsx/.tex ... table09_mos.xlsx/.tex
│
├── 05_scripts/                      # 全部可复现脚本（数据包自包含）
│   ├── run_baseline.py              # 一键跑指定 baseline（子命令分发）
│   ├── compute_metrics.py           # 全部指标统一计算
│   ├── export_xlsx.py               # 指标结果 → 02_raw_data 各 xlsx 的导出器
│   ├── analyze_tc.py                # 板块 3 专项分析
│   ├── analyze_ablation.py
│   ├── make_figures.py              # 一键出全部图
│   ├── make_tables.py               # 一键出全部表
│   ├── verify_package.py            # 数据包完整性校验（跑 MANIFEST 校验）
│   └── README_scripts.md            # 每个脚本的参数说明
│
└── 06_logs/                         # 全部运行日志归档
    └── {experiment_id}.log
```

> 说明：视频帧与生成结果一律不打包（体积巨大且无评审价值）；运行时逐帧算完指标后即释放，只有 raw 数字进 xlsx。若要复现，凭 01_configs + 数据集源 + 05_scripts 重跑即可。

**每个 XLSX 的内部结构统一规范**（所有实验板块遵守）：

每个 xlsx 至少包含：

1. **`_meta` sheet**：数据包版本、schema_version、实验板块说明、生成时间、硬件、git_commit、列名字典
2. **`per_video` sheet**（视频级）：一行 = 一个 (video, method, seed) 组合的完整指标
3. **`frame_level` sheet**（帧级）：一行 = 一帧的指标（原始粒度，量最大）
4. **`summary` sheet**：method × 指标 × mean/std/P50/P95 汇总
5. 板块特有条目的 sheet（如消融变体、用户打分原始表）

**统一列名字典（Field Dictionary，写进每个 _meta sheet）**：

| 列名                                                         | 含义                         | 单位/范围 |
| ------------------------------------------------------------ | ---------------------------- | --------- |
| experiment_id                                                | 实验编号                     | str       |
| dataset                                                      | davis/deepfashion/webvid/... | str       |
| method                                                       | 方法名（9 个之一）           | str       |
| variant                                                      | 消融变体 ID（板块4用）       | str       |
| video_id                                                     | 视频 ID                      | str       |
| frame_idx                                                    | 帧序号（从 0 起）            | int       |
| seed                                                         | 随机种子                     | int       |
| prompt_id                                                    | prompt 集条目 ID             | str       |
| LPIPS                                                        | AlexNet 感知距离             | [0,1] ↓   |
| LPIPS_vgg                                                    | VGG 感知距离                 | [0,1] ↓   |
| TC                                                           | 赛题 L1 时域一致性           | [0,1] ↓   |
| flow_warp_err                                                | 光流 warp 误差               | [0,1] ↓   |
| tLPIPS                                                       | 时序 LPIPS                   | [0,1] ↓   |
| CLIP_score                                                   | 图文一致性                   | [0,1] ↑   |
| flow_mag                                                     | 该帧光流平均幅值             | px        |
| brightness_delta                                             | 帧间亮度差                   | [0,1]     |
| flicker_flag                                                 | 是否闪烁帧（>0.1）           | 0/1       |
| latency_total                                                | 端到端帧延时                 | ms        |
| latency_vae_enc / latency_unet / latency_vae_dec / latency_post | 各阶段延时                   | ms        |
| fps                                                          | 实测帧率                     | Hz        |
| gpu_mem                                                      | 峰值显存                     | MB        |
| scene_category                                               | 场景类型（板块3）            | 4 类      |
| motion_bin                                                   | 运动幅度档                   | 4 档      |
| lag                                                          | 时间间隔（板块3.3）          | 帧        |
| user_id / pair_id / choice / dimension                       | 用户研究字段                 | -         |

**Raw 数据量预估**（支撑"大量数据"要求）：

| xlsx                  | 行数（约）      | 说明                                                         |
| --------------------- | --------------- | ------------------------------------------------------------ |
| exp01_main_comparison | 44,000          | frame_level: 9 方法×3 集×50 视频×30 帧≈40,500；per_video 1,350；seeds 分表 6,750 |
| exp02_latency         | 40,000          | 9 方法×3 分辨率×3 batch×500 帧≈40,500，含四阶段拆分          |
| exp03_tc_analysis     | 60,000          | 时间演化+运动分档+lag 衰减+场景分档的逐帧数据                |
| exp04_ablation        | 10,000          | 15 变体×(50 视频 per_video + 帧级抽样)                       |
| exp05_robustness      | 15,000          | 5 seeds 重复 + prompt 3 套 + 噪声 3 档                       |
| exp06_efficiency      | 2,000           | 参数/MACs/显存/三种加速配置                                  |
| exp07_generalization  | 8,000           | 跨数据集/跨基座/跨任务                                       |
| exp08_user_study      | 4,000           | 60 组×15 人×4 维度原始打分                                   |
| **合计**              | **≈183,000 行** | 纯数字，xlsx 总体积预计 < 100MB                              |

---

## 三、实验板块细化（逐板块：输入→流程→输出）

### 板块 1：主对比实验（P0，核心）

**输入**：3 数据集 × 50 视频 × 9 方法 × 5 seeds（种子取均值，主表报 mean±std）
**流程**：

1. 对每个 (dataset, video, method, seed)：运行 `run_baseline.py --method X --dataset Y --video Z --seed S`
2. 逐帧算完全部指标后即释放帧缓存（不落盘视频），延时数据同步记录
3. `compute_metrics.py` 统一算 7 项指标 → `export_xlsx.py` 写入 `02_raw_data/exp01_main_comparison.xlsx`
4. 汇总：`make_tables.py` 生成主表

**指标清单（7 项主指标）**：

| 指标               | 方向 | 实现                              |
| ------------------ | ---- | --------------------------------- |
| LPIPS (AlexNet)    | ↓    | 官方 lpips 包，逐帧 vs 源帧平均   |
| TC (赛题公式)      | ↓    | 像素 [0,1]，逐帧差 L1，视频内平均 |
| Flow Warping Error | ↓    | RAFT 光流 + 双线性 warp + MSE     |
| tLPIPS             | ↓    | LPIPS(Y^t, warp(Y^{t+1}))         |
| CLIP Score         | ↑    | CLIP ViT-B/32，帧级平均           |
| FID                | ↓    | pytorch-fid，每方法每数据集 2k 帧 |
| FVD                | ↓    | 可选，资源允许则加                |

**产出文件**：

- `02_raw_data/exp01_main_comparison.xlsx`，sheet 结构：
  - `_meta`：字段字典 + 实验快照
  - `frame_level_davis / frame_level_deepfashion / frame_level_webvid`：逐帧 raw（约 4 万行）
  - `per_video`：1350 行视频级 raw
  - `seed_runs`：5 seeds 全量 raw（6750 行）
  - `fid_fvd`：FID/FVD 统计
  - `summary`：method × 指标 × mean/std/P50/P95
- `04_tables/table01_main_comparison.xlsx/.tex`
- `03_figures/fig01_radar.pdf`（质量/一致性/速度/显存四维归一化雷达）
- `03_figures/fig02_per_dataset_bar.pdf`（三数据集 TC+LPIPS 双轴柱状图）

**实验量**：3×50×9×5 = 6750 段视频生成（约 20 万帧）；延时统计每方法额外 500 帧。

---

### 板块 2：实时性约束验证（P0）

**输入**：9 方法，分辨率 {256, 512, 768}，batch {1, 2, 4}，各 500 帧
**流程**：

1. 每配置跑 500 帧，逐帧记录：VAE_encode、UNet、VAE_decode、postproc、total 五列延时
2. P50/P95/P99 统计
3. 与 33.3ms（1/30s）红线对比，标注达标方法

**产出**：

- `02_raw_data/exp02_latency.xlsx`，sheet 结构：
  - `_meta`
  - `frame_level`：9 方法×3 分辨率×3 batch×500 帧 ≈ 4 万行（含五列延时拆分）
  - `percentiles`：P50/P95/P99 统计
  - `summary`：达标判定（✓/✗ 列）
- `04_tables/table02_latency.xlsx/.tex`（含 ✓/✗ 达标列）
- `03_figures/fig03_latency_violin.pdf`
- `03_figures/fig04_latency_resolution.pdf`（延时-分辨率曲线，画红线）

---

### 板块 3：时域一致性深度分析（P1，对应 50% 权重，差异化最大）

**3.1 TC 时间演化**：每视频逐帧 TC 序列 → 全视频均值曲线（我们 vs 5 代表方法）
**3.2 运动幅度分档**：RAFT 光流幅值，按 {0-0.5, 0.5-2, 2-5, >5} px 分 4 bin，各 bin 内算 TC
**3.3 长程一致性**：lag ∈ {1,2,4,8,16} 的 TC 衰减曲线（衡量记忆长度贡献）
**3.4 场景分档**：人工标注 4 类场景（镜头运动/目标运动/遮挡/形变），每类 ≥20 段
**3.5 闪烁率**：亮度突变检测 `|Δbrightness| > τ` 的帧占比

**产出**：

- `02_raw_data/exp03_tc_analysis.xlsx`，sheet 结构：
  - `_meta`
  - `tc_time_evolution`：逐视频逐帧 TC 序列 raw（曲线图数据源）
  - `tc_motion_bins`：motion_bin × method 统计
  - `tc_temporal_lag`：lag × method 统计
  - `scene_categories`：scene_category × method 统计
  - `flicker`：逐帧 flicker_flag raw + 汇总
  - `summary`
- 对应 Fig 5（TC-帧索引曲线）、Fig 6（运动分档柱状）、Fig 7（lag 衰减曲线）、Fig 8（场景对比）
- Table 3：flicker rate

**实验量**：主方法 + 5 代表方法 × 3 数据集 × 50 视频逐帧统计。

---

### 板块 4：消融实验（P0，创新点归因）

假设方法含 Module A（时序记忆库）+ Module B（运动对齐/跨帧一致性注意力）：

| 变体 ID              | 配置                     | 目的              |
| -------------------- | ------------------------ | ----------------- |
| Ours-full            | A+B 全开                 | 完整模型          |
| w/o-A                | 只开 B                   | A 的贡献          |
| w/o-B                | 只开 A                   | B 的贡献          |
| w/o-AB               | 全关（≈StreamDiffusion） | 下界              |
| A-window-2/4/8/16/32 | 记忆窗口消融             | 最佳窗口长度      |
| B-λ-0/0.1/0.5/1/2    | 一致性损失权重           | 最佳 λ            |
| kf-1/4/8             | 关键帧间隔               | 实时性-一致性权衡 |
| steps-1/2/4/8        | 采样步数                 | 少步数鲁棒性      |

**流程**：先在 DAVIS 全 50 视频上跑 15 个变体，选最优配置后在 3 数据集上验证
**产出**：

- `02_raw_data/exp04_ablation.xlsx`，sheet 结构：
  - `_meta`
  - `ablation_main`：变体 × 指标（全量 per_video raw + 汇总）
  - `window_sweep`：A-window 消融
  - `lambda_sweep`：B-λ 消融
  - `kf_sweep`：关键帧间隔
  - `steps_sweep`：采样步数
- Table 4 + Fig 9（窗口长度折线）、Fig 10（λ 折线）、Fig 11（步数折线）
- 每图附"最优配置结论"文字说明

---

### 板块 5：鲁棒性（P2）

**5.1 种子稳定性**：9 方法 × 5 seeds，TC/LPIPS 箱线图（Fig 12）
**5.2 prompt 风格**：3 套 prompt 集（简单词/完整句/风格词）× 3 数据集，指标对比（Table 5）
**5.3 噪声扰动**：输入帧加 σ∈{0, 0.01, 0.05} 高斯噪声，测指标变化
**5.4 视频来源**：真实拍摄 / 合成 / 卡通 各 15 段，跨源对比

**产出**：`02_raw_data/exp05_robustness.xlsx`（sheets：`_meta`、`seed_stability`、`prompt_styles`、`noise_perturb`、`video_sources`、`summary`）

---

### 板块 6：效率与部署（P2）

**内容**：

1. 参数量 / MACs / 显存占用（理论 + 实测）
2. TensorRT 加速、fp16、torch.compile 三种优化下的帧延时
3. 我们新增模块的额外开销 vs TC 收益 → 帕累托前沿（证明边际效率最高）
4. 显存-延时散点图（标注 1/30s 红线和 24GB 显存线）

**产出**：`02_raw_data/exp06_efficiency.xlsx`（sheets：`_meta`、`params_macs_mem`、`accel_configs`、`overhead_vs_gain`、`summary`）；Table 6、Fig 13（延时-显存散点）、Fig 14（收益-开销帕累托）

---

### 板块 7：泛化（P2）

**7.1 跨数据集**：UCF-101 取 20 段 + 手机实拍 10 段，直接推理（Table 7）
**7.2 跨基座**：SD1.5 / SDXL-Turbo / SD-Turbo 三基座（Table 8）
**7.3 跨任务**：风格迁移 / 局部编辑 / 目标替换 三任务 × 各 15 段（Fig 15 可视化 + 指标）

**产出**：`02_raw_data/exp07_generalization.xlsx`（sheets：`_meta`、`cross_dataset`、`cross_backbone`、`cross_task`、`summary`）

---

### 板块 8：主观评估（P1，支撑 20% 主观分）

**设计**：

- 对比对：Ours vs {StreamDiffusion, Rerender, TokenFlow}（前 3 强 baseline）
- 每方法抽 20 段 × 3 数据集 = 60 组对比对
- ≥15 名受试者，网页端盲测（随机左右，AB 按钮）
- 打分维度：时域流畅度 / 画质 / 文本一致性 / 综合偏好

**产出**：

- `02_raw_data/exp08_user_study.xlsx`，sheet 结构：
  - `_meta`
  - `raw_ratings`：匿名化逐条打分 raw（user_id、pair_id、choice、dimension，约 3600 行）
  - `pair_stats`：每组对比对的偏好率 + 95% 置信区间 + 卡方检验 p 值
  - `mos`：MOS 平均分
  - `summary`
- Fig 16（偏好百分比图）、Table 9（MOS）

---

## 四、指标计算代码级规范

```python
# TC（赛题公式，像素[0,1]）
tc_t = torch.abs((Y[t+1] - Y[t]) - (X[t+1] - X[t])).mean()
# 视频级 TC = 各帧 tc_t 平均；数据集级 = 视频级再平均

# LPIPS：官方 lpips 包，net='alex'，逐帧 lpips(Y_t, X_t) 平均

# Flow Warping Error：
flow = raft(X[t], X[t+1]); warp = bilinear_warp(Y[t], flow)
fwe = MSE(warp, Y[t+1])

# tLPIPS = lpips(Y_t, warp(Y_t+1, flow_t->t+1))

# flicker rate = mean( |brightness(Y[t+1]) - brightness(Y[t])| > 0.1 )
```

所有指标实现统一放在 `05_scripts/compute_metrics.py`，单一实现，全部方法共用；`export_xlsx.py` 负责把指标结果按统一规范写入 `02_raw_data/` 下的各 xlsx。

---

## 五、数据包使用说明（README 核心内容）

`00_docs/usage_guide_cn.md` 必须包含：

1. **快速开始**：如何校验数据包完整性（`python 05_scripts/verify_package.py`）、如何打开并阅读各 xlsx
2. **复现指南**：从 `01_configs` + 数据集源 一键重跑任意实验的命令清单
3. **数据浏览**：每个 xlsx 的 sheet 清单 + 全量字段字典（Field Dictionary）
4. **图/表再生成**：`make_figures.py` / `make_tables.py` 用法
5. **扩展指南**：如何加入新方法（新增方法需提交的 5 类文件清单）
6. **常见问题**：指标差异排查、硬件差异对延时的影响说明、† 标注的含义

---

## 六、执行时间线（按依赖排序）

| 阶段 | 内容                                           | 交付物                               |
| ---- | ---------------------------------------------- | ------------------------------------ |
| 1    | 环境锁定 + SD-Turbo 逐帧 trivial baseline 跑通 | 管线校验通过                         |
| 2    | 8 个正式 baseline 全量跑（主对比 + 实时性）    | exp01/exp02 xlsx、Table 1/2、Fig 1-4 |
| 3    | 我们方法全量跑                                 | 主表锁定                             |
| 4    | 消融 15 变体                                   | exp04 xlsx、Table 4、Fig 9-11        |
| 5    | TC 深析 5 项专项                               | exp03 xlsx、Fig 5-8、Table 3         |
| 6    | 鲁棒性/效率/泛化                               | exp05-07 xlsx、Table 5-8、Fig 12-15  |
| 7    | 用户研究（可与 3-6 并行）                      | exp08 xlsx、Fig 16、Table 9          |
| 8    | 数据包组装 + MANIFEST 校验 + 双语文档          | v1.0 发布                            |

---

## 七、质量控制

1. **哈希校验**：MANIFEST.json 覆盖全部文件 SHA256，`verify_package.py` 一键校验
2. **可复现性**：全局种子 + 配置快照写入每个 xlsx 的 `_meta` sheet（含 git_commit、硬件、schema_version）
3. **标注规范**：任何非官方复现值标 †，并在 method_descriptions.md 说明来源
4. **异常处理**：指标超出 3σ 的视频自动标记 `outlier_flag` 列，分析时双版本报告
5. **版本管理**：实验配置变更必须 bump 数据包版本 + changelog 记录
